services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: oec
      POSTGRES_PASSWORD: oec
      POSTGRES_DB: oec_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
